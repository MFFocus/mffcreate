# MffConvert Backend Environment Variables
# Allowed origins for CORS (comma-separated). Supports Netlify domains and local dev.
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://*.netlify.app

# Server binding
HOST=127.0.0.1
PORT=8000
